class FooterHandler {
    static INITIAL_STATE = {
        title: "#FureverAdamski"
    };

    static reducer(state = FooterHandler.INITIAL_STATE, action) {
        return state;
    }
}

export default FooterHandler;
